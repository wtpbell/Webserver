# invalid

cgi ;
cgi off;
cgi on;
cgi error;
cgi on off;
cgi off on;

http {

  # valid 

  cgi on;

  # invalid 

  cgi ;
  cgi error;
  cgi on off;
  cgi off on;

  server {

    # valid

    cgi off;

    # invalid

    cgi ;
    cgi error;
    cgi on off;
    cgi off on;

    location /path {

      # valid

      cgi off;
      cgi on;

      # invalid

      cgi ;

      cgi error;
      cgi on off;
      cgi off on;
    }
  }
}